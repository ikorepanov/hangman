# TODO List
## @apostol_fet
- [ ] Избавиться от большого числа возвращаемых параметров в tuple в init_start_params(). Возможно, с помощью NamedTuple;
- [ ] Убрать указания типов из docstrings;
- [ ] Убрать print'ы из функций, которые выполняют другой функционал (Пр.: init_start_params - возвращает кортеж и пишет пользователю);
- [ ] Изменить проверку, что в слове есть ещё неотгаданные буквы - на основе отображения самих неотгаданных букв (т.к. если отображение поменяется - проверка не будет работать);
- [ ] Добавить обработку возврата пустой строки в get_random_word(). Возможно, с помощью исключения (например, если отсутствует словарь).
## @csatom
- [ ] Добавить отображение уже использованных букв из переменной used_letters вместе с маской;
- [ ] Перенести все константы в отдельный файл (params.py или ENV.py);
- [ ] (?) Добавить возможность ввода слова целиком (добавить проверку на вводимый - один - символ или несколько символов);
- [ ] Явно указать кодировку для выбранного слова в get_random_word(): with DICT_PATH.open("r", encoding="UTF-8") as fhand;
- [ ] Сравнить выбранный способ обращения к фукции open() у объекта класса Path с "обычным": with open(DICT_PATH, "r", encoding="UTF-8") as file;
- [ ] Добавить обработку случаев, когда переменная file может отсутствовать и parents может иметь меньше чем 3 уровня (например, если положить файл в корневую директорию) - при использовании Path(file).parents[2];
- [ ] Исключить ошибку: в функции build_hangman() логическая ошибка в строчке
if 0 <= mistakes <= len(stages) так как len(stages) == 7
и если mistakes = 7, то stages[mistakes] поднимет ошибку IndexError;
- [ ] Добавить отображение загаданного слова в конце игры (если проиграл);
- [ ] Исправить: если запустить файл tools.py в текущем виде, то получим предупреждение SyntaxWarning (это можно исправить: использовать сырые строки в переменны EMPTY, HEAD, BODY, R_HAND, L_HAND, R_LEG, L_LEG. Пр.; EMPTY = r""" ... """);
- [ ] Убрать большое число возвращаемых значений в функции init_start_params() (аналогично первому замечанию @apostol_fet). Например с помощью возврата словаря с возможностью получить параметры по ключам (или - использовать дата-класс:
class Params:
    word: str
    mask: str
    mistakes: int
    used_letters: list);
- [ ] Хранить маску слова не в строке, а сразу в виде списка: ["*", "*", "*", "*", "*"] (тогда в функции open_mask() его не нужно будет всего преобразовывать из строки в список, и потом обратно из списка в строку; функция show_current_state() "скушает" в качестве переменной mask список и выведет все корректно).
