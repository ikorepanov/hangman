# TODO List
## @apostol_fet
- [x] Избавиться от большого числа возвращаемых параметров в tuple в init_start_params(). Возможно, с помощью NamedTuple;
- [ ] Убрать указания типов из docstrings;
- [ ] Убрать print'ы из функций, которые выполняют другой функционал (Пр.: init_start_params - возвращает кортеж и пишет пользователю);
- [ ] Изменить проверку, что в слове есть ещё неотгаданные буквы - на основе отображения самих неотгаданных букв (т.к. если отображение поменяется - проверка не будет работать);
- [ ] Добавить обработку возврата пустой строки в get_random_word(). Возможно, с помощью исключения (например, если отсутствует словарь).
## @csatom
- [ ] Добавить отображение уже использованных букв из переменной used_letters вместе с маской;
- [x] Перенести все константы в отдельный файл (params.py или ENV.py);
- [ ] (?) Добавить возможность ввода слова целиком (добавить проверку на вводимый - один - символ или несколько символов);
- [x] Явно указать кодировку для выбранного слова в get_random_word(): with DICT_PATH.open("r", encoding="UTF-8") as fhand;
- [ ] Сравнить выбранный способ обращения к фукции open() у объекта класса Path с "обычным": with open(DICT_PATH, "r", encoding="UTF-8") as file;
- [ ] Добавить обработку случаев, когда переменная file может отсутствовать и parents может иметь меньше чем 3 уровня (например, если положить файл в корневую директорию) - при использовании Path(file).parents[2];
- [x] Исключить ошибку: в функции build_hangman() логическая ошибка в строчке
if 0 <= mistakes <= len(stages) так как len(stages) == 7
и если mistakes = 7, то stages[mistakes] поднимет ошибку IndexError;
- [x] Добавить отображение загаданного слова в конце игры (если проиграл);
- [ ] Исправить: если запустить файл tools.py в текущем виде, то получим предупреждение SyntaxWarning (это можно исправить: использовать сырые строки в переменны EMPTY, HEAD, BODY, R_HAND, L_HAND, R_LEG, L_LEG. Пр.; EMPTY = r""" ... """);
- [x] Убрать большое число возвращаемых значений в функции init_start_params() (аналогично первому замечанию @apostol_fet). Например с помощью возврата словаря с возможностью получить параметры по ключам (или - использовать дата-класс:
class Params:
    word: str
    mask: str
    mistakes: int
    used_letters: list);
- [x] Хранить маску слова не в строке, а сразу в виде списка: ['*', '*', '*', '*', '*'] (тогда в функции open_mask() его не нужно будет всего преобразовывать из строки в список, и потом обратно из списка в строку; функция show_current_state() "скушает" в качестве переменной mask список и выведет все корректно).
## @grandpraline
- [ ] Сократить docstrings (аналогично второму замечанию @apostol_fet);
- [ ] Изменить (вообще убрать (?)) разбиение на модули (и функции);
- [x] Перенести все константы в отдельный модуль (аналогично одному из замечаний @csatom);
- [ ] Все функции print_...() заменить на обычный print(). В случае необходимости (например, при переносе) - сделать "обёртку для переиспользования(?): так не будут плодиться функции с разыми именами (т.к. меняется только текст);
- [ ] Исключить логику проверки - отгадано ли слово - из функции print_final_message(): она не соответствует своему названию и нарушает принцип единственной ответственности (см. одно из замечаний @aposto_fet);
- [ ] Проверить, что проверка на то, что слово отгадано - уже содержится в функции run_game() в виде проверки на наличие звёздочек в маске;
- [ ] Исправить хардкод количества ошибок (6) в run_game() (например, на случай изменения количества статусов в игре);
- [ ] Решить (каким-то образом) проблему стирания экрана и переноса каретки в начало - без использования хардкода в функции подготовки экрана (например, с помощью стирания экрана целиком всегда и вывода новой "начинки"). Избавиться, таким образом, от game_count в main();
- [ ] Убрать инвертирование из is_not_cyrillic. Заменить на is_cyrillic_letter и возвращать bool. Аналогично: is_not_valid_symbol();
- [ ] Вынести печать из is_cyrillic_letter() и is_already_used();
- [ ] Упростить изложение функций:
`def is_cyrillic(letter: str) -> bool:
    return bool(re.fullmatch('[ёа-я]', letter)):
        `
`def is_already_used(
    used_letters: list[str],
    letter: str,
) -> bool:
    return letter in used_letters`
- [ ] Перенести висельника в ресурсы (из модуля tools) и хранить его, например, рядом со словарём (аналогично одному из замечаний @csatom);
- [ ] Передавать путь к файлу в get_random_word() как аргумент (тогда можно менять файл и не лазить в функцию);
- [ ] (?) Пересмотреть способ выбора случайного слова в get_random_word() (из-за неоптимальности и сложности для чтения). Возможно - использовать что-то из стандартной библиотеки (Пр.: choice);
- [ ] Применить питоническую конструкцию типа `        return word.strip() if word else ''` в get_random_word();
- [ ] Обработать случай отсутсвия слова в get_random_word() и рейзить исключение (аналогично замечанию @apostol_fet);
- [x] Рассмотреть альтернативный вариант изложения функции open_mask:
`
masked_word = list(masked_word)
for index, char in enumerate(word):
    if char == letter:
        masked_word[index] = letter
return ''.join(masked_word)
  `
